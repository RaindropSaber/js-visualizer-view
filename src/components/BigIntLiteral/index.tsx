import { BigIntLiteral as T_BigIntLiteral } from '@babel/types/lib/index.d';

function BigIntLiteral(props: T_BigIntLiteral) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default BigIntLiteral;

