import { ArrayPattern as T_ArrayPattern } from '@babel/types/lib/index.d';

function ArrayPattern(props: T_ArrayPattern) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ArrayPattern;

