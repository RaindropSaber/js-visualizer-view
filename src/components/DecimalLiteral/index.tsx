import { DecimalLiteral as T_DecimalLiteral } from '@babel/types/lib/index.d';

function DecimalLiteral(props: T_DecimalLiteral) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default DecimalLiteral;

