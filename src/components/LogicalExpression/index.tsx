import { LogicalExpression as T_LogicalExpression } from '@babel/types/lib/index.d';

function LogicalExpression(props: T_LogicalExpression) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default LogicalExpression;

