import { DeclaredPredicate as T_DeclaredPredicate } from '@babel/types/lib/index.d';

function DeclaredPredicate(props: T_DeclaredPredicate) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default DeclaredPredicate;

