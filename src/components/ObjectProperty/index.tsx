import { ObjectProperty as T_ObjectProperty } from '@babel/types/lib/index.d';

function ObjectProperty(props: T_ObjectProperty) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ObjectProperty;

