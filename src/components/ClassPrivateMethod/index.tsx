import { ClassPrivateMethod as T_ClassPrivateMethod } from '@babel/types/lib/index.d';

function ClassPrivateMethod(props: T_ClassPrivateMethod) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ClassPrivateMethod;

