import { TSTupleType as T_TSTupleType } from '@babel/types/lib/index.d';

function TSTupleType(props: T_TSTupleType) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TSTupleType;

