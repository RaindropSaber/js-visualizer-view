import { MetaProperty as T_MetaProperty } from '@babel/types/lib/index.d';

function MetaProperty(props: T_MetaProperty) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default MetaProperty;

