import { ObjectPattern as T_ObjectPattern } from '@babel/types/lib/index.d';

function ObjectPattern(props: T_ObjectPattern) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ObjectPattern;

