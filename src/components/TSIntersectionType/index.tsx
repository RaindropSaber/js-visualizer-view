import { TSIntersectionType as T_TSIntersectionType } from '@babel/types/lib/index.d';

function TSIntersectionType(props: T_TSIntersectionType) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TSIntersectionType;

