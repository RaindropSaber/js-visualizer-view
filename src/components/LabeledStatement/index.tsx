import { LabeledStatement as T_LabeledStatement } from '@babel/types/lib/index.d';

function LabeledStatement(props: T_LabeledStatement) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default LabeledStatement;

