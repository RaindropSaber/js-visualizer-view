import { TSIndexSignature as T_TSIndexSignature } from '@babel/types/lib/index.d';

function TSIndexSignature(props: T_TSIndexSignature) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TSIndexSignature;

