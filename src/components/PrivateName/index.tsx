import { PrivateName as T_PrivateName } from '@babel/types/lib/index.d';

function PrivateName(props: T_PrivateName) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default PrivateName;

