import { TypeAnnotation as T_TypeAnnotation } from '@babel/types/lib/index.d';

function TypeAnnotation(props: T_TypeAnnotation) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TypeAnnotation;

