import { StaticBlock as T_StaticBlock } from '@babel/types/lib/index.d';

function StaticBlock(props: T_StaticBlock) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default StaticBlock;

