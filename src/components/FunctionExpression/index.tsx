import { FunctionExpression as T_FunctionExpression } from '@babel/types/lib/index.d';

function FunctionExpression(props: T_FunctionExpression) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default FunctionExpression;

