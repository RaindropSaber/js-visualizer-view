import { UnaryExpression as T_UnaryExpression } from '@babel/types/lib/index.d';

function UnaryExpression(props: T_UnaryExpression) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default UnaryExpression;

