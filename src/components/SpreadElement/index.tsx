import { SpreadElement as T_SpreadElement } from '@babel/types/lib/index.d';

function SpreadElement(props: T_SpreadElement) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default SpreadElement;

