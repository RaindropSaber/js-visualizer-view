import { ClassMethod as T_ClassMethod } from '@babel/types/lib/index.d';

function ClassMethod(props: T_ClassMethod) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ClassMethod;

