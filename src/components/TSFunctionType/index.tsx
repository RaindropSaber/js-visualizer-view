import { TSFunctionType as T_TSFunctionType } from '@babel/types/lib/index.d';

function TSFunctionType(props: T_TSFunctionType) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TSFunctionType;

