import { TSArrayType as T_TSArrayType } from '@babel/types/lib/index.d';

function TSArrayType(props: T_TSArrayType) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TSArrayType;

