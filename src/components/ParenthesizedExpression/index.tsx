import { ParenthesizedExpression as T_ParenthesizedExpression } from '@babel/types/lib/index.d';

function ParenthesizedExpression(props: T_ParenthesizedExpression) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ParenthesizedExpression;

