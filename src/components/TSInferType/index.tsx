import { TSInferType as T_TSInferType } from '@babel/types/lib/index.d';

function TSInferType(props: T_TSInferType) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TSInferType;

