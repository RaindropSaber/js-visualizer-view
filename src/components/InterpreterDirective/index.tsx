import { InterpreterDirective as T_InterpreterDirective } from '@babel/types/lib/index.d';

function InterpreterDirective(props: T_InterpreterDirective) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default InterpreterDirective;

