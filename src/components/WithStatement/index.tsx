import { WithStatement as T_WithStatement } from '@babel/types/lib/index.d';

function WithStatement(props: T_WithStatement) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default WithStatement;

