import { ObjectMethod as T_ObjectMethod } from '@babel/types/lib/index.d';

function ObjectMethod(props: T_ObjectMethod) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ObjectMethod;

