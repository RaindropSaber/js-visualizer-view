import { ClassPrivateProperty as T_ClassPrivateProperty } from '@babel/types/lib/index.d';

function ClassPrivateProperty(props: T_ClassPrivateProperty) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ClassPrivateProperty;

