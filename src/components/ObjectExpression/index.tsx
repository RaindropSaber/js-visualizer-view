import { ObjectExpression as T_ObjectExpression } from '@babel/types/lib/index.d';

function ObjectExpression(props: T_ObjectExpression) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default ObjectExpression;

