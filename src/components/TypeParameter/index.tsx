import { TypeParameter as T_TypeParameter } from '@babel/types/lib/index.d';

function TypeParameter(props: T_TypeParameter) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TypeParameter;

