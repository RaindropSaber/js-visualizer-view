import { InferredPredicate as T_InferredPredicate } from '@babel/types/lib/index.d';

function InferredPredicate(props: T_InferredPredicate) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default InferredPredicate;

