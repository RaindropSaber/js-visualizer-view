import { TSUnionType as T_TSUnionType } from '@babel/types/lib/index.d';

function TSUnionType(props: T_TSUnionType) {
	//const {  } = props;

	return (
		<div></div>
	);
}

export default TSUnionType;

